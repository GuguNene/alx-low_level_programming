#include <stdio.h>

/**
 * main - prits "with proper grammar, but the outcome is 
 * a piece of art," followed newline
 * Return: Always 0.
 *
 */

int main9(void)

{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
